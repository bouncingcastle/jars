[Unit]
Description=Kids Jars scheduled allowance sync
Requires=kids-jars.service
After=kids-jars.service

[Service]
Type=oneshot
ExecStart=/bin/sh -c 'SECRET=$(cat /etc/default/cron-secret); exec /usr/bin/curl -sf -H "x-cron-secret: ${SECRET}" http://127.0.0.1:3000/api/cron'
